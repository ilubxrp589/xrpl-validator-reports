# XRPL Validator — Daily Engineering Report · 2026-08-17

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **8 commits** (xrpl-validator 8)

## Engineering changes

- **fix(xrpl-ledger): a terminal issuer-only change ripples between gateways, never a book** (`04d6c8b`, xrpl-validator)
  toStrand appends a terminal order book for the delivered asset only when the CURRENCY changes (PaySteps.cpp:220-233); its own comment is explicit — "for offer crossing (only) we do use an offer book even if all that is changing is the Issue.account". A PAYMENT whose last hop already holds the delivered currency under a DIFFERENT issuer therefore gets no book at all: the delivery issuer enters normPath as an ACCOUNT …
- **fix(xrpl-ledger): the balance clamps the GROSS, and a pass that misses limitQuality is discarded whole** (`23f1153`, xrpl-validator)
  Two coupled rules behind an IoC cluster one bot repeated every ledger — 11 ledgers, 24 divergences, 77% of everything open. Neither rule fixes it alone.
- **fix(xrpl-ledger): a pseudo-account cannot receive an escrow** (`8e48764`, xrpl-validator)
  `EscrowCreate::preclaim` refuses a pseudo-account destination with tecNO_PERMISSION immediately after the tecNO_DST read, BEFORE the token helper and before the tag test (EscrowCreate.cpp:350-352). It is deliberately not amendment-gated, because every write to a discriminator field is.
- **fix(xrpl-ledger): the strand upper bound charges the intermediate gateway's cut** (`6fb6cf7`, xrpl-validator)
  A payment book step composes its quality with `trIn` — the transfer rate of the book's IN currency when the previous step redeems — in `BookPaymentStep::adjustQualityWithFees` (BookStep.cpp:338-360), and that is what feeds `qualityUpperBound`. Offer crossing deliberately WAIVES the fee ("assume no fee is charged, or the estimate will no longer be an upper bound", BookStep.cpp:519-524); a PAYMENT does not. …
- **feat(probe): DX_FILL on the bridged legs, and lift the saturating key cap** (`8ef6ea5`, xrpl-validator)
  Two instruments, both inert unless their env var is set.
- **fix(xrpl-ledger): the bridged walk never crossed an expired offer either** (`2aa1274`, xrpl-validator)
  `live_head` — the stepper the BRIDGED walk advances with — did not test `Expiration`. The DIRECT walk has tested it since #105776250 and `reap_to_live_head` tests it too; the sibling did not. `hasExpired` is inclusive against the BASE ledger's close time (View.cpp:48; BookStep.cpp builds the stream with `sb.parentCloseTime()`), and rippled's stream collects them as removable — it logs "Removing expired offer" by …

## Docs and operations

- **docs(xrpl-ledger): cite 3.2.1, not the 3.1.3 vendor tree** (`8bbbae3`, xrpl-validator)
- **docs(xrpl-ledger): renumber StrandFlow citations against 3.2.1** (`60c1d81`, xrpl-validator)

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **Batched NAV Redemption Windows for Vaults** (rwa)
  Add sfRedemptionWindowLength and sfNextRedemptionLedger SFields to the SingleAssetVault SLE, plus a bounded RedemptionQueue array of pending claims; a
- **Bounded Recurring Checks for Pull Billing** (payments)
  Add a tfRecurring flag plus sfFrequency (ledgers between cashes), sfNextCashableLedger, sfExpirationLedger, and sfMaxAmount SFields to CheckCreate and
- **Cross-Currency Settlement for NFT Offers** (nft)
  Add an optional sfSendMax SField to NFTokenCreateOffer (buy side) and route NFTokenAcceptOffer's doApply through the existing Flow payment engine inst

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 00:22 | xrpl-validator | `04d6c8b` | fix(xrpl-ledger): a terminal issuer-only change ripples between gateways, never a book |
| 15:57 | xrpl-validator | `8bbbae3` | docs(xrpl-ledger): cite 3.2.1, not the 3.1.3 vendor tree |
| 16:00 | xrpl-validator | `60c1d81` | docs(xrpl-ledger): renumber StrandFlow citations against 3.2.1 |
| 18:21 | xrpl-validator | `23f1153` | fix(xrpl-ledger): the balance clamps the GROSS, and a pass that misses limitQuality is discarded whole |
| 21:26 | xrpl-validator | `8e48764` | fix(xrpl-ledger): a pseudo-account cannot receive an escrow |
| 22:29 | xrpl-validator | `6fb6cf7` | fix(xrpl-ledger): the strand upper bound charges the intermediate gateway's cut |
| 22:59 | xrpl-validator | `8ef6ea5` | feat(probe): DX_FILL on the bridged legs, and lift the saturating key cap |
| 23:24 | xrpl-validator | `2aa1274` | fix(xrpl-ledger): the bridged walk never crossed an expired offer either |

