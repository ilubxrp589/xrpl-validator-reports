# XRPL Validator — Daily Engineering Report · 2026-05-01

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **5 commits** (xrpl-validator 5)

## Engineering changes

- **feat(ffi): silent-divergence detection for tec*/tesSUCCESS mismatches** (`f142a1e`, xrpl-validator)
  The existing divergences.jsonl only records ter*/tef*/tem*/tel* outcomes — our shim returning tesSUCCESS or tec* is treated as agreeing with the network even when the network actually had a different result. That blind spot mattered after the ltCHILD fix: the residual sequence-tracking divergences (29 in 24h, all OfferCreate/TicketCreate) cluster in single- ledger bursts (e.g. L103934068, 27 from one sender …
- **feat(ffi): expose silent divergences in /api/engine + watch_engine.py** (`0431e77`, xrpl-validator)
  Counters parallel to live_diverged_by_type: - live_apply_silent_diverged: total - silent_diverged_by_pair: "{tx_type}/{our_ter}->{net_ter}" → count - silent_diverged_samples: rolling last-50 buffer for the dashboard
- **fix(ffi): set header.closeTime so view.parentCloseTime() isn't always epoch** (`9af0b0a`, xrpl-validator)
  OpenView's open_ledger constructor (OpenView.cpp:105) sets info_.parentCloseTime = base_->info().closeTime — it READS base.closeTime, not base.parentCloseTime. We were setting only header.parentCloseTime; header.closeTime defaulted to 0. Result: view.parentCloseTime() returned the epoch for every tx, and every libxrpl check that consults it broke in a category-specific way: - OfferCreate's hasExpired(view, …
- **feat(ffi): mutation-list divergence detection (BF6C928F class)** (`f0de666`, xrpl-validator)
  Third detection lane after regular and silent: same TER on both sides, different sets of state mutations. Catches cases where our shim and network agreed the tx applied successfully (or hit the same tec*) but the AffectedNodes the tx ended up touching differ.

## Docs and operations

- **chore(watch_engine): remove Live TX Feed block** (`3f7768d`, xrpl-validator)

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 09:33 | xrpl-validator | `f142a1e` | feat(ffi): silent-divergence detection for tec*/tesSUCCESS mismatches |
| 09:47 | xrpl-validator | `0431e77` | feat(ffi): expose silent divergences in /api/engine + watch_engine.py |
| 21:58 | xrpl-validator | `9af0b0a` | fix(ffi): set header.closeTime so view.parentCloseTime() isn't always epoch |
| 22:11 | xrpl-validator | `f0de666` | feat(ffi): mutation-list divergence detection (BF6C928F class) |
| 22:12 | xrpl-validator | `3f7768d` | chore(watch_engine): remove Live TX Feed block |

