# XRPL Validator — Daily Engineering Report · 2026-09-12

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **2 commits** (xrpl-validator 2)
- **1 engine finding fixed** (F266)

## Engine findings fixed

- **fix(xrpl-ledger): F266 — a token escrow finished by its destination creates the missing trust line** (`357975b`, xrpl-validator)
  rippled's `escrowUnlockApplyHelper<Issue>` (EscrowHelpers.h) runs `trustCreate` when the receiver has no line and `createAsset` — which EscrowFinish sets to `destID == accountID_` (EscrowFinish.cpp:368): the destination finishing its own escrow accepts the line. The line lands with limit zero, NoRipple per the destination's DefaultRipple, the reserve on its side, both owner directories and OwnerCount + 1, after …

## Docs and operations

- **chore(watch_engine): ter-miss breakdown counts this run's receipts; upstream panel; no LAN IP** (`b0ec4bd`, xrpl-validator)

## Amendment watch

_Read from the XRP Ledger: the Amendments object as of the last ledger of the day, and the day's EnableAmendment pseudo-transactions._

No amendment held a majority.

Amendments enabled on mainnet: 94.

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **Size-Weighted Owner Reserve for Big Objects** (fees)
  Add a computed SField sfReserveUnits (UInt8, capped at 8) to any variable-length ledger object — starting with MPTokenIssuance, whose XLS-89 metadata 
- **Fixed-Term Amortizing Loans with Rate Oracle** (lending)
  Add sfMaturityCloseTime, sfDayCountBasis, and an optional sfRateOracleID (a PriceOracle keylet) to the Loan object introduced by LendingProtocol, plus
- **Segregated Sub-Accounts with MPC Quorum Custody** (custody)
  Add a new owner-reserved ltSUB_ACCOUNT object (keylet::subAccount(parent, seq)) holding its own XRP balance and trust-line/MPT directory, created by S

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 11:40 | xrpl-validator | `b0ec4bd` | chore(watch_engine): ter-miss breakdown counts this run's receipts; upstream panel; no LAN IP |
| 23:13 | xrpl-validator | `357975b` | fix(xrpl-ledger): F266 — a token escrow finished by its destination creates the missing trust line |

