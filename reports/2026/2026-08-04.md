# XRPL Validator — Daily Engineering Report · 2026-08-04

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **5 commits** (xrpl-validator 5)

## Engineering changes

- **fix(ledger): price an AMM bridge leg at the amount that actually flows** (`b18410f`, xrpl-validator)
  Four changes to the bridged crossing, all read off libxrpl's trace on #105912454 FE592890B233 (24 mutations against mainnet's 17).
- **feat(loop): track rate.py, and stop counting hydration failures as divergences** (`9a877b7`, xrpl-validator)
  rate.py is the native engine's real score — divergences per 1000 tx on UNSELECTED fresh ledgers, as opposed to what is left in a batch of scout-finds, which is the hard residue by construction and barely moves when a common root cause is fixed.
- **fix(ledger): a bridged leg's pool prices the strand only while two are active** (`edf469a`, xrpl-validator)
  #105930662 40FB322EC16C: 6 mutations against 13, all 7 missing ones Modified, nothing extra. Now 45/45 — the ledger is fully clean.
- **fix(ledger): the stream's dead tests apply to the taker's own offer too** (`37d5625`, xrpl-validator)
  #105949459 4A03010A4B1E: 4 mutations against 7. Now 178/178 — the ledger is fully clean.
- **feat(ledger): DX_HEADS/DX_RAWHEADS — per-leg book heads in the bridged walk** (`40cabb2`, xrpl-validator)
  Diagnostics only, both behind the existing `DX_BRIDGE` env var, nothing evaluated unless it is set. Added while chasing #105940336 CA2C624ED031 and kept because they answered two questions in one run each.

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **Bonded Oracle Providers With Slashing** (oracles)
  Add sfBondAmount and sfBondAccount to the PriceOracle ledger object, funded at creation via a new OracleBond transaction that escrows XRP against the 
- **Fixed-Term Amortizing Loans On Vaults** (lending)
  Add a new ltLOAN ledger object (owner-reserved, keylet::loan(vault, borrower, sequence)) carrying sfPrincipal, sfInterestRate, sfMaturityLedger, sfPay
- **Segregated Sub-Accounts With Proof-of-Reserves** (custody)
  Add an lsfSegregated flag and a SubAccountCreate transaction that derives a pseudo-account via keylet::subAccount(parent, index) (extending XLS-64), c

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 01:30 | xrpl-validator | `b18410f` | fix(ledger): price an AMM bridge leg at the amount that actually flows |
| 10:09 | xrpl-validator | `9a877b7` | feat(loop): track rate.py, and stop counting hydration failures as divergences |
| 11:25 | xrpl-validator | `edf469a` | fix(ledger): a bridged leg's pool prices the strand only while two are active |
| 17:46 | xrpl-validator | `37d5625` | fix(ledger): the stream's dead tests apply to the taker's own offer too |
| 18:37 | xrpl-validator | `40cabb2` | feat(ledger): DX_HEADS/DX_RAWHEADS — per-leg book heads in the bridged walk |

