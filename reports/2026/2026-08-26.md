# XRPL Validator — Daily Engineering Report · 2026-08-26

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **2 commits** (xrpl-validator 2)

## Engineering changes

- **fix(node): ws-sync tx-blob fetch validates it received the requested CLOSED ledger — a foreign response (lgrNotFound failover) fed shadow-verify a neighboring ledger's tx set in txid order, 739 phantom seq-divergences across 6 bursts** (`c727fcf`, xrpl-validator)
- **fix(node): ws-sync trusts only validated views — trust gate on every ledger/ledger_entry consumer, and the tx-blob guard reads result.ledger_index (sibling), not result.ledger.ledger_index** (`68bc819`, xrpl-validator)
  A desynced upstream (the reference node, 13:43) served non-validated answers; state writes from them cost a wipe+resync (SEV-1 halt #106560616). With the gates, a sick upstream = hold position, exactly like an unreachable one.

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **Recurring Pull Claims for Payment Channels** (payments)
  Add sfClaimCap (Amount) and sfClaimInterval (UInt32, close-time seconds) to the PayChannel object, settable at PayChanCreate/PayChanFund, plus a new s
- **Oracle-Median Escrow Finish Condition** (oracles)
  Add sfOracleIDs (STVector256, bounded to 8 PriceOracle keylets) and sfMedianCondition (an Amount threshold plus a GTE/LTE comparison flag) to Escrow, 
- **Step-Up Multisig Spend Policy Object** (custody)
  Add a new ltWITHDRAWAL_POLICY object (owner-reserved, keylet::withdrawPolicy(account)) set via a WithdrawalPolicySet transaction carrying sfDailyOutbo

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 12:07 | xrpl-validator | `c727fcf` | fix(node): ws-sync tx-blob fetch validates it received the requested CLOSED ledger — a foreign response (lgrNotFound failover) fed shadow-verify a neighboring ledger's tx set in txid order, 739 phantom seq-divergences across 6 bursts |
| 14:02 | xrpl-validator | `68bc819` | fix(node): ws-sync trusts only validated views — trust gate on every ledger/ledger_entry consumer, and the tx-blob guard reads result.ledger_index (sibling), not result.ledger.ledger_index |

