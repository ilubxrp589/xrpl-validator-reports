# XRPL Validator — Daily Engineering Report · 2026-08-27

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **3 commits** (xrpl-validator 3)

## Engineering changes

- **fix(xrpl-ledger): a tfSell crossing's stream trails the whole in-limit book — trial past live offers, reap the dead — and probe book hydration seeds at the book base** (`e6f0192`, xrpl-validator)
  rippled sizes tfSell with an unbounded-out sentinel: the rev stream trial-consumes every in-limit live offer and keeps stepping, permRm'ing dead offers it lands on; only a beyond-limit live offer ends the walk. Four exit sites taught the rule. Probe-side: book_offers hides expired tips like unfunded ones — seed the ledger_data walk at base24+zeros (the 'marker must exist' constraint is false on 3.3.0). New receipts: …
- **fix(xrpl-ledger): AMMVote applyVote ported for real — wrapped VoteEntry, VoteWeight from live LP balances, lowest-weight eviction, weighted-average TradingFee** (`1478907`, xrpl-validator)
  The stub wrote flat weightless entries; the codec rightly refused to encode them and every fresh-window replay died at the first AMMVote (#106589344, ENCODE-ERR then cascade). First specimen verified by the fresh-window replay sailing through the vote ledger: 66 clean until the next (unrelated) frontier.

## Docs and operations

- **revert(xrpl-ledger): back out the sell-trailing walk pending whole-offer-trial rev semantics — the probe hydration seed stays** (`6d90a33`, xrpl-validator)

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **MPC Threshold-Signature Escrow Release** (custody)
  Register a new fulfillment type in the CryptoConditionsSuite type table for aggregate threshold signatures (e.g. FROST/BLS), and add an owner-reserved
- **Tick-Quantized Trigger Offers** (dex)
  Add a tfTriggerOffer flag to OfferCreate plus two SFields: sfTriggerQuality (a price ratio pre-quantized to the order book's existing TickSize) and sf
- **Expiring MPToken Holder Authorization** (tokens)
  Add an optional sfExpiration (ledger index or close-time) to the MPToken object, settable at MPTokenAuthorize time and refreshable by resubmitting MPT

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 20:29 | xrpl-validator | `e6f0192` | fix(xrpl-ledger): a tfSell crossing's stream trails the whole in-limit book — trial past live offers, reap the dead — and probe book hydration seeds at the book base |
| 21:39 | xrpl-validator | `6d90a33` | revert(xrpl-ledger): back out the sell-trailing walk pending whole-offer-trial rev semantics — the probe hydration seed stays |
| 22:26 | xrpl-validator | `1478907` | fix(xrpl-ledger): AMMVote applyVote ported for real — wrapped VoteEntry, VoteWeight from live LP balances, lowest-weight eviction, weighted-average TradingFee |

