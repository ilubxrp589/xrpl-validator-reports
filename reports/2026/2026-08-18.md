# XRPL Validator — Daily Engineering Report · 2026-08-18

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **14 commits** (xrpl-validator 14)

## Engineering changes

- **fix(xrpl-ledger): a brokered accept must check BOTH offers' Destination** (`140d982`, xrpl-validator)
  In brokered mode rippled tests each offer's `Destination` against the SUBMITTER, in preclaim, before the broker-fee arithmetic: if (auto const dest = bo->at(~sfDestination); dest && *dest != ctx.tx[sfAccount]) return tecNO_PERMISSION; // and the same for so (NFTokenAcceptOffer.cpp:115-126)
- **fix(xrpl-ledger): PaymentChannelCreate refuses DisallowIncomingPayChan and pseudo-accounts** (`74a4d42`, xrpl-validator)
  Two checks rippled makes in `PaymentChannelCreate::preclaim` and we did not (PaymentChannelCreate.cpp:92-113), in its order: tecNO_DST -> lsfDisallowIncomingPayChan -> lsfRequireDestTag -> isPseudoAccount
- **fix(xrpl-ledger): NFTokenModify checks mutability and the issuer's authorised minter** (`39df833`, xrpl-validator)
  `NFTokenModify::preclaim`, after the existence test: if ((getFlags(id) & kFlagMutable) == 0) -> tecNO_PERMISSION if (issuer != account) { minter = issuer's sfNFTokenMinter; if (minter != account) -> tecNO_PERMISSION }
- **fix(xrpl-ledger): cashing a check releases the check's own reserve** (`92d8222`, xrpl-validator)
  rippled states it twice. Preclaim: // "src will have one reserve's worth of additional XRP once the check is // cashed, since the check's reserve will no longer be required" if (value.native()) availableFunds += XRPAmount{ctx.view.fees().increment}; (CheckCash.cpp:175-181)
- **fix(xrpl-ledger): a credential is unlinked from BOTH directories, and the reserve follows acceptance** (`3e02ee6`, xrpl-validator)
  `deleteSLE` (CredentialHelpers.cpp:74-124): delSLE(issuer, sfIssuerNode, !accepted || (subject == issuer)); if (subject != issuer) delSLE(subject, sfSubjectNode, accepted); view.erase(sleCredential);
- **fix(xrpl-ledger): NoRipple on the destination's line blocks the step out of a book** (`db5e428`, xrpl-validator)
  A `DirectStepI` that immediately follows a BOOK step is refused when the SOURCE side of its trust line carries NoRipple (DirectStep.cpp:440-445): if (ctx.prevStep->bookStepBook()) if (sleLine->isFlag((src_ > dst_) ? lsfHighNoRipple : lsfLowNoRipple)) return terNO_RIPPLE;
- **fix: AMM zero-side refusal, NFTokenCancelOffer permissions, default-path book** (`12ddc7c`, xrpl-validator)
  Three rules, gated together on a stable binary.
- **fix(xrpl-ledger): a mint that carries Amount inherits the OFFER's rules too** (`4ae3e03`, xrpl-validator)
  ★ A TRANSACTION THAT IS TWO OPERATIONS INHERITS BOTH SETS OF RULES. `NFTokenMint::preclaim` hands off to `nft::tokenOfferCreatePreclaim` when the transaction carries `Amount` — the mint also creates a sell offer — and that helper refuses a destination which has switched incoming NFT offers off: if (sleDst->isFlag(lsfDisallowIncomingNFTokenOffer)) return tecNO_PERMISSION; (NFTokenHelpers.cpp:885-892)
- **fix(xrpl-ledger): NFT accept judges the buyer's reserve when the transfer creates a page** (`2056087`, xrpl-validator)
  fixNFTokenReserve (NFTokenAcceptOffer.cpp:378-399, transferNFToken): when insertToken has to CREATE a page for the buyer, rippled refuses with tecINSUFFICIENT_RESERVE unless the buyer's balance AS IT STANDS — post-fee, post-price, deliberately NOT the pre-fee form the Mint/CreateOffer gates use — covers accountReserve(ownerCountAfter). Ported into transfer_token so the direct and brokered arms inherit it from the …
- **fix(xrpl-ledger): AMMDeposit re-judges each ACTUAL amount before sending it** (`c91e670`, xrpl-validator)
  rippled's funds test runs TWICE, and we ran neither half fully. Preclaim's balance lambda (AMMDeposit.cpp:230-252) judges the STATED amounts on the pre-fee balance — we had its XRP arm but skipped IOUs entirely; the IOU arm (accountFunds under IgnoreFreeze, issuer never short, no INSUF_RESERVE_LINE split) is now ported. deposit()'s checkBalance (AMMDeposit.cpp:514-538) then re-judges each DERIVED actual right before …
- **fix(xrpl-ledger): AMMVote refuses a voter who is not an LP** (`50d937e`, xrpl-validator)
  AMMVote::preclaim (AMMVote.cpp:59-77), in rippled's order: an emptied pool (LPTokenBalance zero) is tecAMM_EMPTY — new TxResult::AmmEmpty — then a voter holding NO LP tokens is tecAMM_INVALID_TOKENS, "AMM Vote: account is not LP." Judged by ammLPHolds, where a missing line holds nothing. Our preclaim checked only that the voter's account exists, so the vote landed as a fee-only tesSUCCESS (the VoteSlots write needs …
- **fix(xrpl-ledger): OracleSet time rules — tecINVALID_UPDATE_TIME** (`d32160a`, xrpl-validator)
  OracleSet::preclaim (OracleSet.cpp:68-81, :128-130), all three sites in rippled's order, new TxResult::InvalidUpdateTime: 1. LastUpdateTime below the ripple epoch offset (946684800); 2. outside closeTime +/- kMaxLastUpdateTimeDelta (300s) — the harness header's close_time is the parent's close, "the last closed ledger"; rippled bails tecINTERNAL when closeTime < 300, unreachable on a real ledger, so a degenerate …
- **fix(xrpl-ledger): an escrow whose deadline already passed is refused — and the token-escrow funds family** (`d86a69d`, xrpl-validator)
  Two coupled rules, either alone leaves the cluster divergent: 1. doApply's FIRST act (EscrowCreate.cpp:422-428): a CancelAfter or FinishAfter at-or-before the parent close time is tecNO_PERMISSION — after() is strictly `>` (View.cpp:559-562) and the harness header's close_time IS the parent's close. Outranks the XRP funding test and the tag test, both later in doApply. The 14-specimen burst (#106261496/582/583, …
- **fix(xrpl-ledger): mint reserve gate must see the page insert too** (`d57f139`, xrpl-validator)
  rippled snapshots ownerCountBefore at NFTokenMint::doApply ENTRY (NFTokenMint.cpp:281-282) and the tail gate (:331-341) covers everything after it — its own comment: the reserve "is only managed when a new NFT page or sell offer is added". Ours snapshotted AFTER page_insert, so only the mint-offer arm was ever judged and a page-creating mint with no Amount slipped through unjudged.

## XRPL research

### Automated research — candidate amendment ideas

_Ideas generated and prior-art-checked by the XRPL Amendment Board, an automated research tool (read-only board: [iceland.halcyon-names.io](https://iceland.halcyon-names.io/)). They are exploration leads, not proposals._

- **Atomic Flash Loans via Batch and Vault** (lending)
  Add a tfFlashLoan flag to VaultWithdraw permitting collateral-free withdrawal from a SingleAssetVault (ltVAULT) SLE, valid only when the withdrawal is
- **Bounded Sweep of Deletable Dust Accounts** (fees)
  Extend g27's ttOBJECT_SWEEP transaction so its bounded sfSweepIDs vector (max 16 entries) may include AccountRoot keylets in addition to trust-line/ob
- **Oracle-Triggered TokenEscrow Stop Orders** (dex)
  Add three SFields to EscrowCreate when creating a TokenEscrow object — sfTriggerOracle (a PriceOracle keylet), sfTriggerPrice (STAmount threshold), an

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 00:04 | xrpl-validator | `140d982` | fix(xrpl-ledger): a brokered accept must check BOTH offers' Destination |
| 08:18 | xrpl-validator | `74a4d42` | fix(xrpl-ledger): PaymentChannelCreate refuses DisallowIncomingPayChan and pseudo-accounts |
| 12:08 | xrpl-validator | `39df833` | fix(xrpl-ledger): NFTokenModify checks mutability and the issuer's authorised minter |
| 12:32 | xrpl-validator | `92d8222` | fix(xrpl-ledger): cashing a check releases the check's own reserve |
| 14:23 | xrpl-validator | `3e02ee6` | fix(xrpl-ledger): a credential is unlinked from BOTH directories, and the reserve follows acceptance |
| 15:07 | xrpl-validator | `db5e428` | fix(xrpl-ledger): NoRipple on the destination's line blocks the step out of a book |
| 20:09 | xrpl-validator | `12ddc7c` | fix: AMM zero-side refusal, NFTokenCancelOffer permissions, default-path book |
| 20:45 | xrpl-validator | `4ae3e03` | fix(xrpl-ledger): a mint that carries Amount inherits the OFFER's rules too |
| 21:44 | xrpl-validator | `2056087` | fix(xrpl-ledger): NFT accept judges the buyer's reserve when the transfer creates a page |
| 22:27 | xrpl-validator | `c91e670` | fix(xrpl-ledger): AMMDeposit re-judges each ACTUAL amount before sending it |
| 22:48 | xrpl-validator | `50d937e` | fix(xrpl-ledger): AMMVote refuses a voter who is not an LP |
| 23:09 | xrpl-validator | `d32160a` | fix(xrpl-ledger): OracleSet time rules — tecINVALID_UPDATE_TIME |
| 23:27 | xrpl-validator | `d86a69d` | fix(xrpl-ledger): an escrow whose deadline already passed is refused — and the token-escrow funds family |
| 23:55 | xrpl-validator | `d57f139` | fix(xrpl-ledger): mint reserve gate must see the page insert too |

