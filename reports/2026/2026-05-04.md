# XRPL Validator — Daily Engineering Report · 2026-05-04

_Compiled retrospectively on 2026-09-23 from the git history. Scope: XRPL work only — the git history of [xrpl-validator](https://github.com/ilubxrp589/xrpl-validator) and the XRPL Rust SDK, the XRP Ledger itself (amendments) and XRPL research._

## Summary

- **2 commits** (xrpl-validator 2)

## Engineering changes

- **perf(ffi): skip RPC fallback on state.rocks miss in steady state** (`e4500ad`, xrpl-validator)
  Empirical finding from instrumenting per-LedgerEntryType breakdown of RPC fallbacks: across 15,524 fallbacks in a 5k-tx window, rpc_sle_hits was 0 and rpc_sle_misses was 15,524. Every single fallback returned entryNotFound from rippled. State.rocks is complete; the "fallback rate" is purely libxrpl's apply path probing for SLEs that genuinely don't exist on chain ("does this account exist before I create it?", "is …

## Docs and operations

- **chore(watch_engine): rename "RPC FALLBACKS" → "STATE.ROCKS MISSES"** (`1313bca`, xrpl-validator)

## Appendix — commits

| Time | Repo | Commit | Subject |
|---|---|---|---|
| 16:27 | xrpl-validator | `e4500ad` | perf(ffi): skip RPC fallback on state.rocks miss in steady state |
| 21:11 | xrpl-validator | `1313bca` | chore(watch_engine): rename "RPC FALLBACKS" → "STATE.ROCKS MISSES" |

