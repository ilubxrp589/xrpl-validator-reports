- **PermissionDelegationV1_1 lost majority on mainnet.** The network applied `EnableAmendment` with
  `tfLostMajority` at ledger 107,181,569 (2026-09-23 12:47 UTC). It had held a majority since 2026-09-21 and
  would have activated around 2026-10-05; the two-week clock restarts only if it regains majority.
  **BatchV1_1 still holds its majority** (since 2026-09-15 14:06 UTC) and activates around 2026-09-29 14:06 UTC.
- **The second clean 24-hour run.** The live soak started 2026-09-22 11:38 EDT passed its 24-hour mark with zero
  engine receipts — every compared ledger matched rippled byte-for-byte (the first such run was 2026-09-20/21).
- **Batch conformance ahead of activation.** Campaign 23 (devnet) crossed Batch with delegation, MPT,
  credentials, tickets, AccountDelete and LedgerStateFix inners: six engine rules fixed (F389–F394). The subtlest:
  rippled threads an owner's AccountRoot (`threadOwners`) only for nodes an inner itself inserted or erased, in that
  inner's own ApplyStateTable — not for whatever the batch as a whole created. Campaign 24 (devnet) put NFT, offer,
  check, payment-channel, escrow, oracle, DID, AMM, trust-line and AccountSet inners inside Batches, each shaped so
  a later inner uses what an earlier one created: 27/27 byte-exact on the first run.
- **An independent review against the rippled 3.4.0 sources** of the Batch fixes found two more issues, fixed
  test-first (F395, F396): a replay-only pre-image read of raw leaves, and inner transaction ids hashed without the
  codec's canonical spelling of decimal MPT amounts and short-hex `AssetPrice`.
- **Desk checks for the two Batch-only bug classes found so far:** no other transactor copies raw transaction
  `Flags` into a ledger object (where `tfInnerBatchTxn` would leak), and none gates the fee level in preclaim (a
  Batch inner carries `Fee: "0"`).
- **Deployment.** The engine build f7ad693 (findings through F396) replaced the live shadow build at 15:31 EDT after
  its gate (1,589 suite tests and 706 flow-engine tests passing) and two mainnet replay windows (60 and 98 ledgers,
  zero mismatches). The post-deployment health check matched 402 ledgers with no mismatches, and a new 24-hour soak
  began at 15:57 EDT (seed ledger 107,187,825).
- **Receipt canary — first live run.** One drop was planted in the shadow's copy of a mainnet AccountRoot at ledger
  107,188,304; the byte-for-byte compare flagged it and the receipt watcher logged it 255 seconds later. Canary lines
  are never counted as receipts: the check exists to prove that a zero-receipt soak means the comparison was running
  and sensitive. It now runs daily.
- **Soak #26 receipt and finding F397.** At ledger 107,190,696 an NFTokenAcceptOffer was submitted by an account
  other than the offer's Destination, which was also short of the 7 XRP price. Mainnet returned `tecNO_PERMISSION`;
  the engine returned `tecINSUFFICIENT_FUNDS` — a fee-only failure either way. rippled judges the Destination before
  the funds; the engine's accept-offer preclaim now follows rippled 3.4.0's order exactly (F397, with the mainnet
  transaction pinned as a regression vector). Verified and merged; deployment awaits a decision.
- **Second deployment (F397).** Build 1e2d3d3 replaced the live shadow build at 20:51 EDT after its gate (1,594 suite
  tests and 706 flow-engine tests passing) and both mainnet replay windows (zero mismatches). The health check matched
  404 ledgers with no mismatches, and soak #27 began at 21:17 EDT (seed ledger 107,192,792). Soak #26's four receipts —
  all the same NFTokenAcceptOffer case, from one account retrying offers reserved for another — replay byte-exact on
  the fixed build and were archived.
- **Soak #27 receipt and finding F398.** At ledger 107,194,228 an immediate-or-cancel OfferCreate sold 942.58 XRP
  for RLUSD across one maker's five offers at a single quality. The payment engine's first pass consumed four of
  them and left the fifth backed by about 10⁻¹⁴ RLUSD; on the second pass rippled found that offer too small to
  trade and removed it. rippled tells an offer *found* unfunded or tiny (removed) from one that *became* so during
  the payment (skipped) by comparing the owner's funds with the funds read through the strand's "all funds" view,
  whose balance reads apply the payment's deferred-credit table. The engine read that view's raw trust-line balance
  (1.5×10⁻¹⁴ against the adjusted 1.0×10⁻¹⁴), judged the offer to have become tiny, and kept it. Both
  original-funds reads (trust line and XRP) now apply the same adjustments as rippled 3.4.0's `afView` (F398), with
  the mainnet transaction pinned as a regression vector. Verified and merged; deployment awaits a decision.
- **Third deployment (validator-list sources).** Build 74c3b50 replaced the live shadow build at 23:03 EDT after its
  gate (1,596 suite tests and 706 flow-engine tests passing) and both mainnet replay windows (60 and 98 ledgers,
  zero mismatches). The node now fetches its validator list from unl.xrplf.org as well as vl.ripple.com, verifies
  the publisher's signature, and retries with backoff when no list can be fetched instead of running without
  consensus tracking. The list loaded from unl.xrplf.org (35 validators) and the consensus monitor resumed. The
  health check matched 411 ledgers with no mismatches, and soak #28 began at 23:29 EDT (seed ledger 107,194,842).
  Soak #27 ended at the deployment with the single receipt above.
- **Deployment practice.** The node's start-up state sync (19.9 million ledger objects, read from the reference
  rippled node) took 19.4 minutes instead of the usual 5.5–7, because a test build was running on the reference
  node's machine at the same time. Builds and replays there are now held automatically until a deployment's health
  check completes.
